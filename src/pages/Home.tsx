import React from 'react';
import BasicTemplate from '../layout/BasicTemplate';

const Home = () => {
    return (
        <BasicTemplate title={'author'}>
            <h1>Home</h1>
        </BasicTemplate>
    );
};

export default Home;
